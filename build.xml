<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:aether="antlib:org.eclipse.aether.ant"
         xmlns:ball="antlib:ball.util.ant"
         name="ball-upnp" basedir="." default="jar">
  <description>
    ball-upnp.jar
    ---------------------------
    UPnP Implementation Classes

    $Id$
  </description>

  <aether:resolve>
    <dependencies>
      <dependency coords="ball:parent:2.0:xml:compile"/>
    </dependencies>
    <properties/>
  </aether:resolve>
  <import file="${ball:parent:xml}"/>

  <target name="ssdp-discover"
          description="--> Invokes &lt;ssdp-discover/&gt; task."
          depends="jar-classes">
    <taskdef classpathref="build.classpath"
             resource="ball/ssdp/ant/defaults.properties"/>
    <ssdp-discover/>
  </target>

  <target name="main"
          description="--> Invokes &lt;main/&gt; task."
          depends="jar-classes">
    <ball:main classpathref="build.classpath" type="${type}">
      <argument>-p</argument><argument>${port}</argument>
    </ball:main>
  </target>

</project>
